package nl.nanda.service.empty;

import nl.nanda.jpa.transfer.Transfer;

/**
 * We promise the client to return a Transfer, so instead of returning nothing
 * we return this.
 */
public class TransferNull extends Transfer {

}
